# びゅーんパーク 開発メモ(Claude Code用)

4歳児向けのジェットコースター&ウォータースライダー・ライドゲーム。
スマホブラウザで動く一人称3Dライド。**Phase 1〜4実装済み・動作確認済み(2026-07-07)。**

## コンセプト

「遊園地の乗り物を、失敗なし・操作ほぼなしで、何度でも味わえる」

- 4歳児は文字が読めない前提。UIは絵文字のみ
- 失敗・減点・時間制限なし。乗るだけで楽しい+タップでもっと楽しい
- 臨場感は絵の綺麗さではなく演出(FOV・シェイク・音)で作る

## ファイル構成

- `index.html` — HTML + CSS(タイトル/ゴールのオーバーレイ、HUD、紙吹雪、import map、PWAメタ)
- `js/main.js` — Three.jsシーン・コース曲線・カメラ演出・ゲーム状態・⭐永続化
- `js/audio.js` — WebAudio効果音(風切り/水音/カタカタ/⭐ゲット/急降下ひゅ〜/ザッブーン/ファンファーレ)+英語ボイス
- `manifest.json` + `icon-192/512.png` — PWA(アイコンはヘッドレスChromiumで絵文字🎢をスクショして生成)

## デプロイ

- リポジトリ: https://github.com/t11086/byun-park
- 公開URL: **https://t11086.github.io/byun-park/** (GitHub Pages, mainブランチ直配信)
- 手順: `git add` → `git commit` → `git push` で自動デプロイ(反映まで1〜3分)
- デプロイ後は `curl "https://t11086.github.io/byun-park/js/main.js?v=$(date +%s)"` で反映確認

## ローカル実行・検証

- `python3 -m http.server 8788`(このディレクトリで)→ http://localhost:8788
  **注意: 8787はぱくぱくフレンズのサーバーが常駐していることがあるので8788を使う**
- スマホ実機: 同じWi-Fiで `http://<MacのIP>:8788`(IPは `ipconfig getifaddr en0`)
- 構文チェック: `node --input-type=module --check < js/main.js`(importがあるので --input-type=module 必須)
- 描画の自動確認: playwright-core + キャッシュ済みChromium(`~/Library/Caches/ms-playwright/chromium_headless_shell-*`)で
  実走スクリーンショット確認。ボタンはpulseアニメのため tap に `{ force: true }` が必要

## 技術方針

- **Three.js(WebGL2)+ ビルドなし**(ES modules + import map、CDN直読み)
- `CatmullRomCurve3` + `TubeGeometry` でコース生成、曲線に沿ってカメラ追従(公式rollercoasterデモと同方式)
- 物理エンジン不使用。「エネルギー保存っぽい式(低いほど速い)×摩擦係数」の自前計算
- 音はWebAudioで全合成(外部音源ファイルなし)。英語ボイスのみ speechSynthesis(en-US)
- 負荷対策: ローポリ+InstancedMesh、フォグ、pixelRatio最大2

## 設計ルール(ユーザー確認済み・変更しないこと)

- **先頭にキャラ(おともだち)を乗せない**。一度実装したが「視界の邪魔」とのことで廃止(2026-07-07)。一人称の疾走感を最優先
- 失敗・減点・時間制限なし。⭐は取り逃しても何も起きない
- 文字UIなし(絵文字のみ)、ボタンは大きく
- スマホ縦画面前提、`100dvh`
- 3D酔い対策: カメラのロール(傾き)なし、下りでは注視点を持ち上げて地平線を残す

## 現在のゲーム構成

- タイトル(上空旋回で全景+⭐累計)→ 🎢/🛝 をタップ → ライド → ゴール(紙吹雪+⭐の数+🔁もういっかい/🏠ホーム)
- 🎢: 登りカタカタ(約11秒)→頂上0.8秒ため→急降下(FOV 66→94+シェイク+風音)。約35秒
- 🛝: 摩擦係数0.5でマイルド、水音ループ、青リングくぐり+水しぶき、最後にザッブーン(splash状態0.9秒→ゴール)
- ⭐は画面のどこをタップしても前方20m以内なら取れる
- ⭐累計はlocalStorage(`byunpark.totalStars`)に保存し、タイトルに表示
- 英語ボイス: "Let's go!" / "You did it!"(rate .9 / pitch 1.4)

## 実装で得た知見(2026-07-07)

- 雲などの白いオブジェクトにMeshLambertMaterialを使うと、下面が半球ライトの地面色(緑)に染まる → MeshBasicMaterial
- 急降下中はコース接線方向をそのまま見ると画面全体が地面になる → 下りでは注視点を持ち上げる(`horizonLift`)
- リフト速度4m/sだと頂上まで25秒かかり4歳児には長すぎる → 9m/s(約11秒)
- まくら木は実寸(1.7m幅)だと一人称視点で巨大に見える → 1.15mに縮小
- Pointsは素だと四角い粒 → 放射グラデのCanvasTextureを貼る。カメラ直近に湧かせると巨大化するので2.5m以上先に
- 半透明チューブだけでは速度感ゼロ → 一定間隔のリング(Torus)をくぐらせると効く
- カメラの注視点が遠い(10m)と、チューブの急カーブでコーナーをショートカットして壁越しに外を見てしまう → スライダーはlookAhead=5m

## 次のアップデート候補(ユーザー未承認のアイデア)

- ⭐がたまると新コース解放(夜の星空コース、虹のスライダー等)
- ⭐ゲット時の英語カウント("One! Two! Three!")
- 実機での体感調整(速度・揺れ・音量)

## 未決定事項(ユーザー確認待ち)

- アプリ名(仮: びゅーんパーク)
