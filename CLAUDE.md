# びゅーんパーク 企画書(Claude Code用)

4歳児向けのジェットコースター&ウォータースライダー・ライドゲーム。
スマホブラウザで動く一人称3Dライド。**Phase 1(コースター1本の技術検証)実装済み・動作確認済み(2026-07-07)。**

## ファイル構成

- `index.html` — HTML + CSS(タイトル/ゴールのオーバーレイ、HUD、import map)
- `js/main.js` — Three.jsシーン・コース曲線・カメラ演出・ゲーム状態
- `js/audio.js` — WebAudio効果音(風切り/カタカタ/⭐ゲット/急降下ひゅ〜/ファンファーレ)

## ローカル実行・検証

- `python3 -m http.server 8788`(このディレクトリで)→ http://localhost:8788
  **注意: 8787はぱくぱくフレンズのサーバーが常駐していることがあるので8788を使う**
- スマホ実機: 同じWi-Fiで `http://<MacのIP>:8788`(IPは `ipconfig getifaddr en0`)
- 構文チェック: `node --input-type=module --check < js/main.js`(importがあるので --input-type=module 必須)
- 描画の自動確認: playwright-core + キャッシュ済みChromium(`~/Library/Caches/ms-playwright/chromium_headless_shell-*`)で
  スクリーンショット確認済み。スタートボタンはpulseアニメのため tap に `{ force: true }` が必要

## 実装で得た知見(2026-07-07)

- 雲などの白いオブジェクトにMeshLambertMaterialを使うと、下面が半球ライトの地面色(緑)に染まる → MeshBasicMaterial
- 急降下中はコース接線方向をそのまま見ると画面全体が地面になる → 下りでは注視点を持ち上げて地平線を残す(`horizonLift`)
- リフト速度4m/sだと頂上まで25秒かかり4歳児には長すぎる → 9m/s(約11秒)
- まくら木は実寸(1.7m幅)だと一人称視点で巨大に見える → 1.15mに縮小

## コンセプト

「遊園地の乗り物を、失敗なし・操作ほぼなしで、何度でも味わえる」

- 4歳児は文字が読めない前提。UIは絵文字と絵のみ
- 失敗・減点・時間制限なし。乗るだけで楽しい+タップでもっと楽しい
- 1ライド 60〜90秒。飽きる前に終わり「もういっかい!」を誘発する長さ

## 技術方針(2026-07-07 検討)

- **Three.js(WebGL2レンダラー)+ ビルドなし**(ES modules + import map、CDN直読み)
- 理由:
  - `CatmullRomCurve3` + `TubeGeometry` でコース生成、曲線に沿ってカメラを進める定番手法(公式rollercoasterデモと同方式)がそのまま使える
  - 物理エンジン不使用。「曲線上の進行度 + 勾配で加減速」の自前計算で軽量・破綻なし
  - Unity/Godot Web書き出しは数十MBのロードで4歳児が待てないため不採用
  - WebGPUはSafari普及待ちのためWebGL2で全スマホ対応
- 描画負荷対策: ローポリ+トゥーン調(ライト1灯+ vertex color 中心)、フォグで描画距離を隠す、`renderer.setPixelRatio` は最大2でキャップ、ジオメトリは起動時に生成して使い回し
- 音: WebAudioで効果音を合成(外部音源ファイルなし)。英語ボイスは speechSynthesis(en-US)

## ゲーム構成(案)

### 画面フロー

1. **タイトル** — 🎢ロゴ+大きな「あそぶ」ボタン(絵文字)
2. **のりもの選び** — 🎢ジェットコースター / 🛝ウォータースライダー の2枚看板をタップ
3. **キャラ選び** — 動物のおともだちから1匹(🐰🐻🐼…)。先頭車両に乗る
4. **ライド本編**(下記)
5. **ゴール演出** — 紙吹雪+拍手+集めた⭐のカウントアップ → 「もういっかい?」

### ライド本編(臨場感の核)

- 一人称〜肩越し視点。カメラはコース曲線に追従
- **ジェットコースター**: カタカタ登り(期待をためる)→ 頂上で一瞬ポーズ → 急降下びゅーん → ループ・カーブ → ゴール
- **ウォータースライダー**: チューブの中をくねくね → 水しぶきパーティクル → 最後にザッブーン(大きな水柱)
- **遊び要素はタップ1種類だけ**: コース上に浮かぶ ⭐ や 🎈 が近づいてきたら画面のどこでもタップでゲット。当たり判定は激甘(近くにあれば取れる)。取り逃しても何も起きない
- ゲットするたび英語で "Star!" "Balloon!" + キラン音

### 臨場感の演出(絵の綺麗さより効かせどころ)

- **FOV演出**: スピードが上がるとカメラの画角を広げる(疾走感が段違いに出る)
- **カメラシェイク**: 降下中に微振動、着水でドンと揺れ
- **音の演出**: 登りのカタカタ音 → 頂上で無音 → 降下でゴォー(ノイズ+ピッチ変化)。スライダーは水音ループ
- **スピードライン**: 高速時に画面端へ流れる風の線パーティクル
- **キャラのリアクション**: 降下で「わーい!」と両手を上げる、水しぶきで目をつぶる

### 育成・くりかえし要素(案・未確定)

- ライドごとに集めた⭐がたまる → 新コース解放(ぐるぐるコース、夜の星空コース、虹のスライダー等)
- 集めたキャラがゴールで出迎えてくれる

## 4歳児向け設計ルール

- 文字UIなし(絵文字+アイコンのみ)、ボタンは大きく
- 失敗なし・急かさない・毎回褒める
- スマホ縦画面前提、`100dvh`、PWA対応予定
- 3D酔い対策: カメラのロール(傾き)は控えめに、ループは1コースに1回まで、急カーブは速度を落とす

## 開発フェーズ

1. ~~**Phase 1 — 技術検証**: カメラ追従+FOV演出+音の気持ちよさを確認~~ **完了(2026-07-07)**
   - タイトル(上空旋回)→ 登りカタカタ → 頂上ポーズ → 急降下(FOV拡大+シェイク+風音)→ ⭐タップ収集 → ゴール🎉 まで一周動作
2. **Phase 2 — コースター磨き込み**: キャラ(先頭車両+おともだち)、演出強化、実機での体感調整
3. **Phase 3 — スライダー追加**: 水表現(半透明チューブ+しぶきパーティクル)
4. **Phase 4 — くりかえし要素**: ⭐収集の永続化、コース解放、PWA化、デプロイ

## 未決定事項(ユーザー確認待ち)

- アプリ名(仮: びゅーんパーク)
- キャラのデザイン(絵文字ベースか、簡単な3Dプリミティブ組み合わせか)
- デプロイ先(GitHub Pages想定)
